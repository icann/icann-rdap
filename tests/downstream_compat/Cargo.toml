[package]
name = "downstream_compat"
version = "1.0.0"
edition = "2021"
publish = false
rust-version.workspace = true

[dependencies]
icann-rdap-common = { path = "../../icann-rdap-common" }

strum = { version = "0.28", features = [ "derive" ] }
